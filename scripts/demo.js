let _ = require('lodash'); //common practice to name it with underscore. no default loction is required since node knows its default location for its packages

console.log(_.random(1,1000)); //another package is nodemon which tracks changes automatically and you do not have to write node everytime to run it in terminal 
